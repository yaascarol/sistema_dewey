CREATE TABLE Funcionario(
ID INTEGER PRIMARY KEY AUTOINCREMENT,
Nome VARCHAR(100) NOT NULL, 
Email VARCHAR (100) NOT NULL,
Senha VARCHAR (100) NOT NULL
);

CREATE TABLE Genero ( 
ID INTEGER PRIMARY KEY AUTOINCREMENT, 
Categoria_Livro VARCHAR(100) NOT NULL
); 

CREATE TABLE Autor (
AutorID INTEGER PRIMARY KEY AUTOINCREMENT,
NomeAutor VARCHAR(50) 
);

CREATE TABLE Livros (
ID INTEGER PRIMARY KEY AUTOINCREMENT,
Nome_Livro VARCHAR(100) NOT NULL, 
AutorID INTEGER NOT NULL,
Genero VARCHAR(50) NOT NULL,
Ano_Publicacao DATE NOT NULL, 
Editora VARCHAR(50) NOT NULL,
Categoria_ID INTEGER NOT NULL, FOREIGN KEY (AutorID)
REFERENCES Autor(AutorID), FOREIGN KEY (Categoria_ID) REFERENCES Categoria(ID)

INSERT INTO Funcionario (Nome, Email, Senha) VALUES 
('Lais Lívia', 'laislivia@gmail.com', '858560');